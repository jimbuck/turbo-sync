{
  "name": "turbo-sync",
  "version": "1.0.0",
  "description": "CLI tool to update package.json files for non-JS projects in a turborepo repository",
  "main": "src/index.ts",
  "bin": {
    "turbo-sync": "./bin/turbo-sync"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest"
  },
  "dependencies": {
    "@types/debug": "^4.1.12",
    "commander": "^8.0.0",
    "debug": "^4.4.0",
    "fast-glob": "^3.3.3",
    "fast-xml-parser": "^5.2.1"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "vitest": "^3.1.2"
  }
}
